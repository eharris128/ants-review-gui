const isContractInteractable = (
  web3Instance,
  networkID,
  antsReviewInstance,
  accounts
) => web3Instance && networkID && antsReviewInstance && accounts;

export default isContractInteractable;
